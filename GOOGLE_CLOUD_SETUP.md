# คู่มือการตั้งค่า Google Cloud Vision API

## 🔑 API Key ที่คุณได้รับ

```
AIzaSyCm3OI14Pw8UaEkl7K6-EKy_otXaxnne1I
```

## ⚙️ ขั้นตอนการตั้งค่า

### 1. เพิ่ม API Key ในไฟล์ `.env`

เปิดไฟล์ `api/.env` และเพิ่มบรรทัดนี้:

```env
GOOGLE_CLOUD_API_KEY=AIzaSyCm3OI14Pw8UaEkl7K6-EKy_otXaxnne1I
```

### 2. Restart API Server

หลังจากเพิ่ม API key แล้ว ต้อง restart server:

```bash
cd api
npm start
```

หรือถ้าใช้ nodemon:
```bash
npm run dev
```

### 3. ทดสอบระบบ

1. ไปที่ `http://localhost:5173/dashboard/student/lessons/693c0e39bcc9acee0ad7949c`
2. เขียนตัวอักษร "ก" ให้ชัดเจน
3. กดปุ่ม "ส่งคำตอบ"
4. ✅ ถ้าเขียนถูก ควรจะขึ้น "เก่งมาก! เขียนเสร็จแล้ว"
5. ❌ ถ้าเขียนมั่วๆ ควรจะขึ้น "ลองใหม่"

## 🔍 การ Debug

ดู Console Logs ใน terminal ที่รัน API server:

```javascript
Vision API Response: {
  detectionsCount: 1,
  fullText: 'ก',
  targetWord: 'ก'
}
Cleaned detected text: ก
```

## ⚠️ ปัญหาที่อาจเจอ

### API Key ไม่ทำงาน

```
Google Cloud Vision API authentication failed. Please check your API key.
```

**แก้ไข:**
- ตรวจสอบว่า copy API key ถูกต้อง (ไม่มีช่องว่าง)
- ตรวจสอบว่า enable Cloud Vision API แล้วใน Google Cloud Console
- ลอง generate API key ใหม่

### Quota เกิน

```
Google Cloud Vision API quota exceeded.
```

**แก้ไข:**
- ตรวจสอบ quota ใน Google Cloud Console
- รอจนถึงเดือนหน้า (free tier reset)
- เพิ่ม billing account

## 💰 ค่าใช้จ่าย

- **1,000 requests แรก**: ฟรี ทุกเดือน
- **หลังจากนั้น**: ~$1.50 ต่อ 1,000 requests

สำหรับการทดสอบและ classroom เล็กๆ ควรจะไม่เกิน free tier
