# MongoDB Configuration
# 
# For MongoDB Atlas (Cloud) - YOUR CONNECTION STRING:
DATABASE_URL="mongodb+srv://<db_username>:<db_password>@bearthai.vhek1d9.mongodb.net/bearthai?retryWrites=true&w=majority"
#
# OR for local MongoDB:
# DATABASE_URL="mongodb://localhost:27017/bearthai"

# JWT Configuration
JWT_SECRET="your_jwt_secret_key_here"
JWT_EXPIRES_IN="7d"

# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Anthropic Claude API (สำหรับตรวจสอบลายมือ)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Email Configuration (Optional - Set EMAIL_ENABLED=true to enable)
EMAIL_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587                    # 587 for STARTTLS, 465 for SSL
SMTP_SECURE=false                # false for 587 (STARTTLS), true for 465 (SSL)
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=BearThai@gmail.com
APP_NAME=BearThai